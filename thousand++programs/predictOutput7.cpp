#include <iostream>
using namespace std;

int main(){
    int x = 10, y = 20;
    if(x == y);
        cout << x << " -> " << y << '\n';
    return 0;

    // -- output -- // 
    
    // predictOutput7.cpp:6:15: note: put the semicolon on a separate line to silence this warning
    // 1 warning generated.
    // 10 -> 20
}